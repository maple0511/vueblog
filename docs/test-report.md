# 软件测试报告

## 测试策略

- 单元测试：认证状态、AI 元数据解析、权限规则。
- Web 集成测试：注册、登录、受保护接口、文章和评论生命周期。
- 前端测试：Pinia 状态、Markdown 清洗、流式解析。
- 部署测试：Compose 配置、容器健康检查、数据持久化和公网访问。

## 关键黑盒用例

| 编号 | 场景 | 预期 |
|---|---|---|
| B01 | 合法注册 | 201，返回 JWT 和用户信息 |
| B02 | 重复用户名/邮箱 | 409 |
| B03 | 错误密码登录 | 401 |
| B04 | 未登录发布文章 | 401 |
| B05 | 修改他人文章 | 403 |
| B06 | 关键词与标签筛选 | 返回符合条件的分页结果 |
| B07 | AI 服务未配置 | 文章保存成功，AI 状态最终为 FAILED |
| B08 | AI 问答超出文章内容 | 回答“该文章未提供相关信息” |
| B09 | 超过每日 AI 配额 | 429 |
| B10 | 恶意 Markdown | 前端清除危险 HTML |
| B11 | 管理员隐藏帖子 | 公开列表不再展示，管理员后台仍可查看 |
| B12 | 用户选择兴趣标签 | 推荐页只返回匹配标签的已审核文章 |
| B13 | 管理员停用成员 | 被停用成员无法继续登录 |

## 白盒关注点

- `AuthService` 的唯一性、密码匹配和令牌生成分支。
- `PostService` 的作者所有权、标签替换和 AI 降级分支。
- `PostService` 的审核状态过滤、管理员审核和推荐匹配分支。
- `UserPreferenceService` 的兴趣标签保存、默认选项和推荐入口分支。
- `AiMetadataService` 的 JSON、摘要长度、标签数量和失败状态分支。
- `AiStreamService` 的完成、中止、异常和日志分支。

## 2026-06-23 实际执行结果

| 项目 | 结果 |
|---|---|
| 后端 `mvn clean verify` | 通过，3 个集成测试零失败 |
| 前端 `npm run build` | 通过 |
| 前端 `npm test` | 通过 |
| Compose 配置校验 | 通过 |
| 公网首页 `http://1.94.218.30:18000` | HTTP 200 |
| 未登录发布文章 | HTTP 401 |
| 注册、JWT、文章发布、评论 | 通过 |
| 中文标题关键词搜索 | 命中 1 条，总数与分页正确 |
| AI 未配置降级 | 文章保存成功，状态由 PENDING 变为 FAILED，人工摘要保留 |
| MySQL/Flyway | MySQL 8.4 启动成功，V1 迁移成功 |
| 容器健康检查 | MySQL、后端、前端均配置健康检查 |

公网验收使用模拟文章和临时测试账号，不包含真实个人信息或 AI 密钥。

## 千问真实调用验收

2026-06-23 在公网部署环境启用阿里云百炼 `qwen3.7-plus`，结果如下：

| 用例 | 实际结果 |
|---|---|
| AI 摘要 | 状态 `READY`，生成 121 字中文摘要 |
| AI 自动标签 | 生成“实验室、校园服务、预约指南”3 个标签 |
| AI 写作助手 | 标题建议通过 SSE 返回，收到 `done` 事件 |
| 文章内问答 | 正确回答工作日开放和提前一天预约等文章事实 |
| 超出文章问题 | 对收费问题返回“该文章未提供相关信息” |
| 深度思考响应 | 忽略 `reasoning_content`，仅向前端发送最终 `content` |
| 调用治理 | METADATA、WRITING、QUESTION 成功写入脱敏调用日志 |

## 2026-06-24 管理员与推荐扩展验证

| 项目 | 结果 |
|---|---|
| 后端 `mvn test` | 通过，6 个测试零失败 |
| 前端 `npm test` | 通过 |
| 前端 `npm run build` | 通过 |
| 注册后兴趣引导路由 | 前端注册成功后跳转 `/onboarding` |
| 推荐接口 | 保存“美食”偏好后仅返回同标签已审核文章 |
| 管理员帖子审核 | `HIDDEN` 后公开文章列表不再展示该帖子 |
| 管理员成员列表 | `/api/admin/users` 仅管理员可访问 |
